;; Example 8: Line-prefix stripping.
;; Diagram expressions can be written as comments in various languages.
;; Leading //, --, or # (single, followed by whitespace) are stripped.
;; ## remains the diagram's own end-of-line comment marker.

// A := box("Input")
// B := r-box("Process")
// C := box("Output")
-- D := text("Source")
-- E := text("Sink")

// D -> A -> B -> C -> E
